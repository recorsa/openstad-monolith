{% extends './base.njk' %}

{% set pageTitle = 'Foutmelding' %}
{% set contentClass = 'error' %}

{% block content %}
	<div id="errorMessage">
	{% if status == 403 %}
		<h1>{{message}}</h1>
		
		<p>
			De toegang tot deze pagina is geblokkeerd omdat u niet voldoende rechten bezit.
		</p>
		
		{% if not user.isLoggedIn() %}
		<p>
			U bent momenteel niet ingelogd. Als u normaal wel toegang heeft tot deze pagina,
			probeert u het dan na <a href="/account/register">inloggen</a> nogmaals.
		</p>
		{% endif %}
	{% elif status == 404 %}
		<h1>{{message}}</h1>
		
		<p>Helaas. De pagina waar u naar op zoek bent bestaat niet (meer).</p>
		
		<p>Gebruik het menu bovenaan deze pagina om naar een andere pagina te navigeren.</p>
	{% else %}
		<h1>{{status}}: {{friendlyStatus}}</h1>
		
		{{errorMessage | nl2br | safe}}
	{% endif %}
	</div>
{% endblock %}